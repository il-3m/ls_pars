# Универсальный парсер ЕИС (Unified Parser)

Этот скрипт объединяет две части парсера в единый рабочий процесс:
1. **Поиск ссылок** (из `link_finder.py`) - находит контракты по заданным критериям
2. **Парсинг данных** (из `eis_parser.py`) - извлекает данные из страниц контрактов

## Возможности

- **Единый интерфейс**: Не нужно запускать два разных скрипта
- **Автоматический цикл**: Нашел ссылку → обработал → добавил в таблицу → следующая ссылка
- **Накопление данных**: Все распарсенные строки собираются в одну итоговую таблицу
- **Два режима работы**: GUI (графический интерфейс) и консольный режим
- **Гибкие настройки**: Можно настроить таймауты, фильтры, форматы экспорта
- **Расчет НМЦК**: Встроенный инструмент для расчета начальной максимальной цены контракта
- **Ручной подбор позиций**: Возможность вручную формировать список позиций для НМЦК

## Запуск

### Графический интерфейс (GUI)

```bash
python unified_parser.py --gui
```

Или просто без аргументов (GUI запускается по умолчанию):

```bash
python unified_parser.py
```

### Консольный режим

```bash
python unified_parser.py --search "АЗИТРОМИЦИН" --max-contracts 10 --out-csv result.csv
```

#### Параметры консольного режима:

| Параметр | Описание | Пример |
|----------|----------|--------|
| `--search` | Поисковый запрос (МНН) | `"АЗИТРОМИЦИН"` |
| `--date-from` | Дата с (YYYY-MM-DD) | `2024-01-01` |
| `--date-to` | Дата по (YYYY-MM-DD) | `2024-12-31` |
| `--max-contracts` | Максимум контрактов | `20` |
| `--moscow-only` | Только Москва и МО | (флаг) |
| `--rosunimed-only` | Только Росунимед | (флаг) |
| `--archive-dir` | Папка для архива | `archive` |
| `--out-csv` | Выходной CSV файл | `export/result.csv` |
| `--out-xlsx` | Выходной XLSX файл | `export/result.xlsx` |
| `--headed` | Браузер с окном (не headless) | (флаг) |
| `--timeout-ms` | Таймаут загрузки (мс) | `90000` |
| `--expand-rounds` | Раунды раскрытия элементов | `5` |

### Примеры команд

```bash
# Поиск по МНН за последние 3 месяца
python unified_parser.py --search "ИБУПРОФЕН" --max-contracts 50

# Только Москва, экспорт в Excel
python unified_parser.py --search "ПАРАЦЕТАМОЛ" --moscow-only --out-xlsx result.xlsx

# Полный цикл с настройками таймаутов
python unified_parser.py --search "МЕТАМИЗОЛ" --timeout-ms 120000 --expand-rounds 7
```

## Как это работает

### Этап 1: Поиск ссылок
1. Открывается страница поиска ЕИС
2. Формируется запрос с параметрами (МНН, даты, регион)
3. Проходятся все страницы результатов
4. Извлекаются ссылки на карточки контрактов
5. Ссылки преобразуются в формат для парсинга

### Этап 2: Парсинг данных
1. Для каждой найденной ссылки:
   - Открывается страница контракта в Playwright
   - Раскрываются все элементы "Объекты закупки"
   - Извлекаются данные таблицы (наименование, цена, производитель и т.д.)
   - Данные добавляются в общий список
2. После обработки всех ссылок:
   - Данные экспортируются в CSV
   - Опционально создается XLSX файл

### Структура выходных данных

Таблица содержит следующие поля:
- Наименование
- Категории ЛС
- ОКПД2
- Страна происхождения
- МНН (ГРЛС)
- Торговое наименование
- Регистрационное удостоверение (РУ)
- Форма выпуска
- Дозировка
- Количество в потреб. единице
- Цена за единицу (₽)
- Сумма (₽)
- НДС
- Держатель РУ
- Производитель
- Страна производителя
- Вид первичной упаковки
- И другие технические поля

## Расчет НМЦК

Во вкладках **"НМЦК"** и **"НМЦК ручной подбор"** реализован функционал расчета начальной максимальной цены контракта:

### Вкладка "НМЦК"
1. Введите данные для расчета:
   - Цены за ед. измерения по 1, 2, 3 КП (коммерческим предложениям)
   - Объем в ед. измерения (требуемое количество)
2. Используйте кнопки расчета:
   - **"НМЦК по объему"** — находит 3 позиции с количеством, наиболее близким к указанному объему
   - **"НМЦК минимальная"** — находит 3 позиции с минимальной ценой
   - **"НМЦК приближенная к КП"** — находит 3 позиции со средней ценой, наиболее близкой к средней по введенным КП
   - **"НМЦК оптимальная"** — оптимальный алгоритм с приоритетом "вписаться в цену" и минимизацией разброса по объему
   - **"НМЦК идеальная"** — расширенный алгоритм подбора
3. Результаты отображаются в таблице "Результат расчета НМЦК (3 позиции)"
4. Блок "Итоговые данные" показывает:
   - Минимальная цена по КП
   - Минимальная по ЕИС
   - Дельта цен (абсолютная и процентная)
   - Макс. отклонение по объему (абсолютное и процентное) — **выделяется бордовым цветом**, если объем выходит за пределы диапазона сопоставимости

### Вкладка "НМЦК ручной подбор"
1. Двойной клик по строке в таблице результатов добавляет позицию в таблицу ручного подбора
2. Поля ввода данных синхронизированы между вкладками "НМЦК" и "НМЦК ручной подбор"
3. Блок "Итоговые данные" показывает аналогичные показатели, включая макс. отклонение по объему с бордовой подсветкой при выходе за пределы диапазона сопоставимости
4. Кнопки "📄 Скачать Excel" и "📋 Скопировать" позволяют экспортировать результаты

### Диапазон сопоставимости
В настройках можно указать **диапазон сопоставимости для НМЦК** (по умолчанию = 3). Это значение определяет допустимое отклонение по объему в разах. Например, при объеме 1000 и диапазоне 3 допустимый диапазон составляет [333, 3000]. Если хотя бы одна позиция выходит за эти пределы, значение макс. отклонения выделяется бордовым цветом.

## Детали позиции

При двойном клике по любой ячейке строки открывается модальное окно **"Детали позиции"** с полной информацией:
- Все значения видны полностью (длинные значения переносятся и не обрезаются)
- Значения можно выделить и скопировать (Ctrl+C)
- Кнопка "🗑️ Удалить позицию" (красная) позволяет удалить позицию через подтверждение
- Кнопка "➕ Добавить в НМЦК" добавляет позицию в таблицу ручного подбора

## Требования

- Python 3.8+
- PyQt5 (для GUI)
- Selenium + webdriver-manager (для поиска ссылок)
- Playwright (для парсинга)
- pandas, openpyxl (опционально, для XLSX)

### Установка зависимостей

```bash
pip install PyQt5 selenium webdriver-manager playwright pandas openpyxl
playwright install chromium
```

## Отличия от оригинальных скриптов

| Характеристика | link_finder.py | eis_parser.py | unified_parser.py |
|----------------|----------------|---------------|-------------------|
| Поиск ссылок | ✅ | ❌ | ✅ |
| Парсинг данных | ❌ | ✅ | ✅ |
| Экспорт | Только ссылки | Таблица | Таблица |
| Интерфейс | PyQt5 | Tkinter | PyQt5 |
| Автоматизация | Ручной запуск 2 скриптов | Один скрипт | Полный цикл |
| Расчет НМЦК | ❌ | ❌ | ✅ |

## Логирование

Все действия записываются в файл `unified_parser_log.txt`:
- Время начала/окончания
- Найденные ссылки
- Ошибки парсинга
- Статистика по строкам

## Решение проблем

### Ошибка "libtk8.6.so not found"
Это ошибка tkinter, не влияет на работу GUI (используется PyQt5). 
Для консольного режима можно игнорировать.

### CAPTCHA при поиске
При обнаружении CAPTCHA парсер делает паузу. Рекомендуется:
- Уменьшить количество контрактов за один проход
- Добавить задержки между запросами
- Использовать прокси

### Нет данных в таблице
Возможные причины:
- Контракт не содержит раздел "Объекты закупки"
- Изменилась структура сайта ЕИС
- Требуется больше времени на загрузку (увеличьте `--timeout-ms`)

## Лицензия

Использование парсера должно соответствовать условиям использования сайта zakupki.gov.ru
